@model List<WishList.Models.Item>

<h1>Wishlist</h1>

<a asp-action="create"><button class="btn btn-primary">Add item</button></a>

<ul>
    @foreach (var item in Model)
    {
        <li>@item.Description <a asp-action="delete" asp-route-id="@item.Id">delete</a></li>
    }
</ul>